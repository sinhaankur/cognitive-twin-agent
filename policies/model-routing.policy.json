{
  "version": "1.0.0",
  "objective": "Route requests across local models by latency, risk, and reasoning depth.",
  "models": {
    "primary": {
      "provider": "ollama",
      "name": "qwen2.5:7b",
      "role": "default_reasoning",
      "comment": "Installed + instruct-tuned. Was qwen3:14b (NOT installed) — that mismatch made every route fail and is why Vera 'didn't understand'. Repoint to a real model.",
      "targetLatencyMs": 3500
    },
    "fastFallback": {
      "provider": "ollama",
      "name": "qwen2.5:3b",
      "role": "low_latency_and_battery",
      "targetLatencyMs": 1800
    },
    "deepPlanner": {
      "provider": "ollama",
      "name": "qwen2.5:14b",
      "role": "high_complexity_planning",
      "comment": "The heavy brain for genuinely complex/high-risk planning + drive. Loaded ON DEMAND by Ollama only when a rule escalates here — everyday turns stay on the light 7B, so Vera is powerful WITHOUT being heavy by default. (Ollama unloads it after idle, freeing RAM.)",
      "targetLatencyMs": 9000
    },
    "companion": {
      "provider": "ollama",
      "name": "qwen2.5:7b",
      "role": "emotional_companion",
      "comment": "Emotional / check-in turns. Uses the same capable reasoner — Vera's WARMTH comes from her persona (persona.py _INNATE + persona.json), not a separate model. (empathia-tiny is a different project, deliberately NOT wired in here.)",
      "targetLatencyMs": 3500
    }
  },
  "routingRules": [
    {
      "id": "rule_companion",
      "comment": "Emotional / check-in / companionship turns go to the warm Empatheia model — the 'companion' hat. Reasoning, planning, and VS Code drive never route here.",
      "when": {
        "intent": ["companion", "emotional", "check_in", "reflect"]
      },
      "useModel": "companion"
    },
    {
      "id": "rule_low_power",
      "comment": "Device constraints win first — a throttled/low-battery device must not run the big model.",
      "when": {
        "deviceState": ["battery_saver", "thermal_throttle"]
      },
      "useModel": "fastFallback"
    },
    {
      "id": "rule_high_risk",
      "comment": "Any high-risk request gets the careful planner, regardless of how short it is.",
      "when": {
        "riskLevel": ["high"]
      },
      "useModel": "deepPlanner"
    },
    {
      "id": "rule_deep_path",
      "comment": "Complex planning, even at lower risk, benefits from the deeper model.",
      "when": {
        "taskComplexity": ["high"]
      },
      "useModel": "deepPlanner"
    },
    {
      "id": "rule_fast_path",
      "comment": "Everything else — quick, low-risk asks — goes to the default model.",
      "when": {
        "taskComplexity": ["low", "medium"],
        "riskLevel": ["low", "medium"]
      },
      "useModel": "primary"
    },
    {
      "id": "rule_default",
      "comment": "Catch-all so nothing ever falls through unrouted.",
      "when": {},
      "useModel": "primary"
    }
  ],
  "escalation": {
    "maxSelfRetries": 2,
    "retryOnLowConfidenceBelow": 0.55,
    "handoffToDeepPlannerAfterFailures": 1
  },
  "guardrails": {
    "requireReversiblePlanForHighRisk": true,
    "allowCloudFallback": false
  }
}
