"""
Cognitive Twin — a local-first personal AI agent.

A working agent core (model + skills + loop + CLI) that runs entirely on your own
machine via Ollama. Inspired by OpenJarvis (github.com/open-jarvis/OpenJarvis);
original implementation.

The legacy subsystems under ../src (oauth, ipc, menubar, multimodal) are earlier
scaffolding kept as future layers — this package is the part that actually runs.
"""

__version__ = "0.1.0"
