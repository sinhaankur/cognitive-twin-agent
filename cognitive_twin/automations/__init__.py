"""
automations — set up automations without code, safely.

A recipe is a shareable JSON automation you import + REVIEW before it runs
(recipe.py). Record-and-replay lets you demonstrate a task once and save it as a
recipe (record.py). Nothing here runs on import or watches your screen
autonomously; every automation is reviewed, dry-run first, and replays only what
you showed it. On-device, sealed.
"""
